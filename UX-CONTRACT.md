# 任职管理系统 UX Contract

视觉规范见 [DESIGN.md](DESIGN.md)。本文件是所有后台与移动端流程的行为约定。

## Canonical UI Map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
|---|---|---|---|---|
| Form | PHP 语义表单 + `app.js` 表单控制器 | 本文件 | 新增 / 编辑 / 设置页内嵌 | PHP 语法 + 浏览器键盘流程 |
| Select/Listbox | 原生 `select` | 本文件 + DESIGN.md | native | 中文选项 + 键盘操作 |
| Date | 原生 `input[type=date]` | 本文件 | native date-only | 浏览器日期输入检查 |
| Dialog | `app.js` Dialog 控制器 | 本文件 | 表单 / 二维码 / 确认 | 焦点循环 + Escape + 恢复焦点 |
| Toast | 页面 flash + `#app-live-region` | 本文件 | success / error | live region 检查 |
| Table | 语义 table + `.table-wrap` | 本文件 + DESIGN.md | 舒适密度 / 横向滚动 | 表头、分页、窄屏检查 |
| Search | `.global-search` / `.filter-bar` | 本文件 | 全局 / 页面筛选 | 清除、聚焦、URL 状态 |
| Scrollbar | `public/assets/extra.css` | DESIGN.md | 全局 / 稳定 gutter | 浏览器计算样式 |
| CRUD | `public/index.php` 路由处理器 | 业务文档 + 本文件 | 返回列表 / 设置页停留 | 创建、编辑、失败流程 |

## 新增、编辑与保存

- 后台新增和编辑统一在弹窗中完成；设置页保留页面内表单。
- 创建或编辑成功后由服务端回到所属列表，并通过页面 flash 告知结果。
- 提交时按钮尺寸不变，显示“正在保存…”并阻止重复提交；失败时保留已输入内容。
- 长表单在弹窗正文中滚动，标题和操作区可达；移动端转为全屏弹窗。
- 表单发生修改后关闭弹窗，需要通过应用内确认框决定继续填写或放弃修改。

## 状态、空结果与错误

- 状态必须使用中文文字，颜色只作辅助。
- 空数据说明该区域会出现什么，并在有权限时给出新增入口。
- 搜索无结果应提示清除搜索或重置筛选。
- 权限不足与确实无数据分开表达；服务端错误使用稳定的中文信息。

## 删除、停用与重置

- 系统当前不提供通用硬删除。
- 停用、取消、移除、轮换二维码、重置评分均使用应用内确认框。
- 轮换二维码必须说明旧码立即失效；评分重置必须说明需要重新填写。
- 后台确认成功后刷新当前列表，并用 flash 反馈。

## 数据集导航

- 小型本地部署采用前端分页，每页 12 条，仅在数据超过一页时显示分页器。
- 已提交的搜索和筛选条件以 URL 参数为事实来源。
- 改变搜索条件回到第一页；上一页和下一页在边界保持可见但禁用。
- 分页器显示“第 X–Y 条，共 N 条”，并提供中文可访问名称。

## 权限与会话

- 服务端权限判断是唯一安全边界；前端只隐藏无权入口，不替代鉴权。
- 会话过期由服务端返回登录页；未保存表单保留仅限当前页面生命周期。
- 候选人手机号等个人信息在列表中保持脱敏。

## 响应式与无障碍

- 目标为 WCAG 2.2 AA；所有可操作元素支持键盘、可见焦点和至少 24×24px 命中区，主要触控目标约 44px。
- 720px 以下侧栏收窄；560px 以下后台弹窗全屏，H5 页面保持自然文档滚动。
- 支持 200% 缩放、`prefers-reduced-motion` 和强制色彩模式。
- 中文界面使用 `zh-CN`、公历和 Asia/Shanghai 业务时区；输入法组合期间不触发搜索提交。

## 行为清单

| 操作 | 触发 | 等待 | 成功 | 失败恢复 |
|---|---|---|---|---|
| 保存表单 | “保存…”按钮 | 按钮忙碌且禁用 | 返回列表并显示成功信息 | 保留字段并显示错误 |
| 搜索 | Enter / 筛选按钮 | 保持工具栏尺寸 | URL 更新、结果刷新 | 保留查询并允许重试 |
| 危险操作 | 文字危险按钮 | 确认框内提交 | 刷新列表并播报 | 确认框保留上下文 |
| 打开二维码 | “二维码”链接 | 弹窗显示加载状态 | 展示可打印、可下载二维码 | 跳转到独立二维码页作为降级 |
